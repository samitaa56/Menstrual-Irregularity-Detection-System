// src/config.js

// Centralized API configuration to make cross-device testing easier.
// Dynamically detect the hostname (localhost or PC IP)
export const API_BASE_URL = "http://localhost:8000/api";

export default API_BASE_URL;
